<div class="title-list">
    <div class="right-top">
        <div id="default-panel">
            <i class="iconfont icon-search" data-title="検索<% if (theme.shortcutKey != false) {%> 快捷键 i<%}%>"></i>
            <div class="right-title"><%= __('all_articles') %></div>
            <i class="iconfont icon-file-tree" data-title="アウトラインビューに切り替える<% if (theme.shortcutKey != false) {%> ショートカット: w<%}%>"></i>
        </div>
        <div id="search-panel">
            <i class="iconfont icon-left" data-title="背面"></i>
            <input id="local-search-input" autocomplete="off"/>
            <label class="border-line" for="input"></label>
            <i class="iconfont icon-case-sensitive" data-title="大文字・小文字を区別する"></i>
            <i class="iconfont icon-tag" data-title="タグ"></i>
        </div>
        <div id="outline-panel" style="display: none">
            <div class="right-title">概要</div>
            <i class="iconfont icon-list" data-title="記事一覧への切替"></i>
        </div>
    </div>

    <%- partial('_partial/tag') %>
    <% if (theme.searchAll) { %>
    <div id="local-search-result">

    </div>
    <% } %>
    <nav id="title-list-nav">
        <% site.posts.forEach(function(post, i){  %>
        <a <% if(post.top){%>id="top"<%}%> class="<%= __('all_articles') %> <% post.categories.forEach(function(category, i){ %><%=category.name%> <% }) %>"
           href="<%- url_for(post.path) %>"
           data-tag="<% post.tags.forEach(function(tag, i){ %><%=tag.name%><% if (i+1<post.tags.length){%>,<%}})%>"
           data-author="<% if(theme.author && theme.author.on==true && post.author) {%><%=post.author %><%}%>" >
            <span class="post-title" title="<%=post.title %>"><%=post.title %></span>
            <span class="post-date" title="<%= date(post.date, 'YYYY-MM-DD HH:mm:ss')%>"><%= date(post.date, 'YYYY/MM/DD') %></span>
        </a>
        <% }) %>
        <div id="no-item-tips">

        </div>
    </nav>
    <div id="outline-list">
    </div>
</div>
